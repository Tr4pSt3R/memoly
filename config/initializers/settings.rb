#Each multiplier currently given in days is converted to seconds
# Settings.defaults[:multiplier] = [1,7,30,90,180].map { |x| x*86400 }

# #Time tolerance allows us to determine the proximity of due memoid releases
# Settings.defaults[:time_tolerance] = 1*60*60 #1hour*60minutes*60seconds