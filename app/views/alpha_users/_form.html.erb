<%= form_for(@alpha_user) do |f| %>
  <div>
    <% if @alpha_user.errors.any? %>
      <div id="error_explanation">
        <h2><small><%= pluralize(@alpha_user.errors.count, "error") %> prohibited this email from being saved:</small></h2>

        <ul>
        <% @alpha_user.errors.full_messages.each do |msg| %>
          <li><small><%= msg %></small></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  </div>

    <div class="input-append">
      <%= f.email_field :email, 
                        # :class => "span6",
                        :id => "appendedInputButton", 
                        :type => "email", 
                        :placeholder => "email..."%>
      <%= f.submit "Remind me", 
                        :class => "btn btn-info" %>
    </div>
    
  
<% end %>
