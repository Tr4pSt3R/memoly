<div>
	<% if notice %>
		<p class="notice"> <%= notice %> </p>
	<% end %>
	<% if alert %>
		<p class="alert"> <%= alert %> </p>
	<% end %>
</div>
<div class="container-fluid">
	<div class="row-fluid">
		<div class="span5">
			<header>
				<h3><%#= image_tag("icons/user3.svg", :size => "25x25") %><%= @user.username %></h3>
				
				<h4>My Public Memoids</h4>
					<% @user.memoids.each do |m| %>
						<p><%= m.note if m.public == true %></p>
					<% end %>

				<h4>My Private Memoids</h4>
					<% @user.memoids.each do |m| %>
						<p><%= m.note if m.public == false %></p> 
					<% end %>
			</header>
		</div>
		<div class="span7">
			<h1>
				<span id="head0"></span>
			</h1>
			<%#= render 'memoids/form', current_user.memoids.build  %> 
			
			<div class="span-box0 span12 offset0">
				<%= form_for [@user, @memoid = @user.memoids.build] do |f| %>
					<% if @memoid.errors.any? %>
						<div id="error_explanation">
						    <h2><%= pluralize(@memoid.errors.count, "error") %> prohibited this Memoly from being saved:</h2>

						    <ul>
							    <% @memoid.errors.full_messages.each do |msg| %>
							      <li><%= msg %></li>
							    <% end %>
						    </ul>
						</div>
					<% end %>
					<%= f.text_area :note, :size => "40x4", :placeholder => "Enter memoid..."  %>
					<p>
						<%= f.check_box :public %>
						<%= f.label :public, "Public", :class => "btn" %>
					</p>
					<div>
						<header><h4>Settings</h4></header>
						<%= f.select_time @memoid%>
						<%#= f.time_select '@memoid', :value => 'release_time' %>
						<%#= binding.pry %>
					</div>
					<p><%= f.submit "Add memoid", :class => "btn btn-info" %></p>
				<% end %>
			</div>

			<p>
				<%#= link_to '<- Back', user_path %>
			</p>
		</div>
	</div>
</div>







<!-- <p id="notice"><%= notice %></p>

<p>
  <b>Firstname:</b>
  <%= @user.firstname %>
</p>

<p>
  <b>Lastname:</b>
  <%= @user.lastname %>
</p>


<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
 -->