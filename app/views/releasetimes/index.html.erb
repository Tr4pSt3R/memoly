<h1>Listing releasetimes</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @releasetimes.each do |releasetime| %>
  <tr>
    <td><%= link_to 'Show', releasetime %></td>
    <td><%= link_to 'Edit', edit_releasetime_path(releasetime) %></td>
    <td><%= link_to 'Destroy', releasetime, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Releasetime', new_releasetime_path %>
